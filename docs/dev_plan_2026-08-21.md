# Next-session plan (written 2026-08-20, evening after the v0.3.0 release)

Priority-ordered. Items 1–2 are bench work gated on hardware being out; 3+ are
code work that can run any time.

## 1. The trusted signal source (arrives ~2026-08-21) — make the vertical axis ABSOLUTE

This is the headline. One measurement closes the release note's biggest caveat.

- [ ] Run `python3 scripts/verify_scope_cal.py` against the new source and set
      `SCOPE_CAL_SOURCE_SCALE` in `scope_cal.h`. **Do not hand-edit table rows**
      (`tests/test_scope_cal.c` fails the build if you do).
- [ ] While the source is hooked up, re-measure the **provisional gain rows**
      (ranges 4/8/9, where channels disagreed 25–48% off one fixed amplitude) —
      a source with real amplitude control can serve both ends of the ladder.
- [ ] Sensitive ranges: the 0.4–8 mV/count rows need **PC12/PA15 HIGH at small
      amplitudes** (noted 2026-08-17, never run).
- [ ] If the source reaches high enough in frequency: re-fit timebase code
      **`0x0D`** (119,678 S/s, still PROVISIONAL — the ESP32 couldn't place a
      peak that far up). Use `scripts/capture_scope_records.py` so the rates
      stay single-sourced from `scope_timebase.c`.
- [ ] Characterize the ESP32 bench source against the trusted one while both
      are out — one table of actual-vs-commanded makes the old rig usable as a
      cross-check forever.

## 2. Quick bench checks (5 minutes each, do while flashed)

- [ ] **Settings permanence** — user-deferred from 2026-08-20. First real
      exercise of the verified-write path (P0.4): change timebase + theme,
      power cycle, confirm restore; then `flash diag` for error counters.
- [ ] Confirm v0.3.0-coldtrace binary boots clean from the release asset
      (download it back, sha256, flash) — validates the artifact users get,
      not just our build tree.

## 3. P1 audit items (docs/structural_audit_2026-08-20.md)

In the audit's recommended order:

- [ ] **Decorative controls — coupling first** (wrong-unit class: UI shows
      AC/DC but PC12 never moves; the per-range relay table drives it LOW =
      AC in every arm, wrong for DC work). Fix via the `fpga_apply_timebase()`
      template: single entry point, record what's in force, UI refuses to lie.
- [ ] **Channel enable mask** (PC1/PC2) — same template, second verse.
- [ ] Two USART2 TX engines → one owner.
- [ ] Silent queue drops → counters + shell visibility.
- [ ] Task monitoring: 8 of 10 tasks unmonitored by the health check-in.

## 4. Release follow-through (low effort, high goodwill)

- [ ] Watch #18 and the release for feedback; if **caldump values** arrive,
      open the promised dedicated collection issue (pristine-unit factory-cal
      comparison — data we cannot generate ourselves).
- [ ] Star #100 watch; repo-pulse early in the session.
- [ ] Mac side: `git fetch --tags` at next sync so both machines know v0.3.0.

## 5. Parked science (deliberately deferred, not dropped)

- **Bit-bang vs no-0x05 bisect** — which single variable actually broke the
  config wall. Matters for a faster hardware-SPI config path, not for shipping.
- **CDC culprit single-variable test** — flash `guest-configB` (bit-bang config
  WITHOUT warm-handoff defines): CDC up ⇒ hardware-SPI config path is the
  culprit; down ⇒ FPGA_WARM_HANDOFF_TEST. (Details in CLAUDE.local.md.)
- **fpga.c split** — deferred until trigger-modes work touches it.

## 6. FPGA dev board (arrives in the same package)

Strategy decided 2026-08-20 (see discussion): **hybrid — stock-RE becomes the
spec, our own bitstream becomes the product.** Key fact shaping everything:
the SSPI config handshake (0x11/0x15/0x3B/0x3A) is the Gowin **silicon config
engine** (UG290), not fabric logic — any valid GW1N-2 `.fs` loads through our
existing bit-bang loader unchanged. The runtime protocol (SPI3 reg file,
0x04/0x05 reads, arm) is fabric and exists only in stock's design.

Milestone ladder:
- **M0** — apicula/yosys/nextpnr hello world on the dev board (toolchain proof).
- **M1** — hello world SRAM-loaded on the 2C53T via `fpga_bitbang_config_sequence`
  with our own `.fs` payload. Reversible by design: SRAM config only, power
  cycle restores the NV meter image. Prerequisite: pin-safe constraints
  (start all-inputs + one observable output; netlist recon + #18 tracing give
  the IOB map). Success = DONE_FINAL set on a bitstream we built.
- **M2** — minimal SPI3 slave register file speaking the existing protocol
  subset (reg 0x01, 0x08, opcode 0x04 returning a counter ramp) — MCU driver
  unchanged, `m_capture.py` sim as the executable spec to diff against.
- **M3** — real ADC capture path (the hard block: timing closure).
- **Side quest the dev board uniquely enables:** the parked bit-bang-vs-
  hardware-SPI bisect, on a bench part with full logic-analyzer visibility.

### The config-transport bisect (dev board + logic analyzer)

Why it matters: stock 2C53T configures through the **hardware SPI3 peripheral
at /2** (Exp E, CTRL1 BR=0) — the very peripheral that failed for us for two
months — while the working Build B is a transplant of the **2C23T sibling's**
GPIO bit-bang loader. So the fix is not "GPIO beats peripheral"; it is a
bundle of three changes that have never been separated. Build A (hardware SPI
at /256, byte-identical framing, FAILED) already excludes raw clock speed.

Three suspects, one experiment each, single-variable, on the dev board first
(zero risk) then confirmed on the 2C53T (power cycle restores NV):

- **B1 — prelude reads.** Hardware-SPI transport + the V0.4 prelude
  (`0x11 → 0x13 → 0x41` before `0x12`/`0x15`). Works ⇒ the engine needs a
  read transaction to sync before config entry.
- **B2 — the missing `0x05`.** Hardware-SPI transport, our original prelude,
  ERASE_SRAM omitted. Works ⇒ our `0x05` was actively poisoning entry, not
  inert. Cross-check: bit-bang transport WITH `0x05` added — if that now
  fails, `0x05` is confirmed the whole story.
- **B3 — the waveform.** Bit-bang transport, byte-identical sequence, and a
  logic-analyzer diff against the hardware-SPI capture: inter-byte gaps,
  CS setup/hold vs first/last clock edge, clock idle state across CS, edge
  rates (GPIO drive vs AF). If B1 and B2 both fail, this is what's left —
  and it is the one layer byte-level fidelity work was structurally blind to.
  (HiLetgo 24 MHz is ample at /256 and for bit-bang; NOT for /2 — keep the
  hardware-SPI leg at /256 for capture runs.)

Decision rule: run B1 and B2 on the dev board in one sitting (they are
firmware-only variants of existing builds); only if both fail invest in the
B3 capture rig. Any positive ⇒ re-run on the 2C53T, then retire the bit-bang
path or keep it with a documented reason. Bonus stake: the CDC failure
correlates with the hardware-SPI config path (CLAUDE.local.md, replicated on
a second unit in PR #13) — whichever suspect wins here is also the leading
candidate for that mechanism.
